{
  "name": "routing-app",
  "private": false,
  "version": "1.0.0",
  "description": "An Angular routing example",
  "repository": "https://github.com/lillylangtree/routing.git",
  "license": "MIT",
  "devDependencies": { 
    "http-server": "^0.6.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}